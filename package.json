{
  "name": "apge-pdf-form-filler",
  "version": "1.0.0",
  "private": true,
  "description": "Static TypeScript APG&E PDF filler that runs fully in the browser and deploys to GitHub Pages.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
