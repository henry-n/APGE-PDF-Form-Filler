import { defineConfig } from "vite";

export default defineConfig({
  // Relative paths make the app work on GitHub Pages project URLs like:
  // https://your-username.github.io/your-repo-name/
  base: "./"
});
